This folder consists of 4 subfolders containing all the assignments.

1) Place this folder in desired directory.
2) Make sure that all the permissions are given to this folder for the scripts to be executed properly.
3) Enter the details host, port, database name, username and password in "db.conf" file before executing the scripts.
3) The packages required for each assignment are specified in the readme.txt file of each of them.
4) To create database and tables a script is written "db.php" which is present in all the 4 assignment folders. Go to the folder from terminal and run "php db.php". 
